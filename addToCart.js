import { Builder, By, Key } from "selenium-webdriver";
import { should } from "chai";
should();

// // **Describe block
// describe("Regression Test", function () {
//    //**Adding to cart valid & invalid Test Cases
//   it("1) Verify adding one item to the cart", async function () {});
//   it("2) Verify adding three itens to the cart", async function () {});
//   it("3) Verify adding the same item three times to the cart", async function () {});
//   it("4) Verify removing one item from the cart on the 'Products' page'", async function () {});
//   it("5) Verify removing one item from the cart on the 'Your cart' page", async function () {});
//   it("6) Verify removing three items from the cart", async function () {});
//   it("7) Verify removing more than one item from the cart", async function () {});
// });
